# Evaluation Lifecycle

```text
prompt / retrieval / model change
  -> offline eval
  -> groundedness review
  -> citation check
  -> entitlement test
  -> red-team test
  -> stage deploy
  -> canary
  -> online watch period
```

## Key lesson
In LLMOps, evaluation is continuous and multi-layered. It is not a single benchmark run.
